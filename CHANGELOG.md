# 更新日志

本文件记录 dsh-dock 各版本的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)。

## v0.11.2 — 2026-09-14

### 修复

- **从会话输入区的用量 chip 打开用量记录，面板立即按当前会话显示结果，无需再点「查询」**：
  此前面板挂载时两条初始查询并发竞争——按上次暂存条件的那条查询（先扫历史、较慢）最后到达，
  把先返回的会话筛选结果覆盖回全量/旧条件，看起来像没有自动查询。现在会话入口带入的
  sessionId 直接并入唯一一条初始查询（会话下拉框初值即选中该会话），不再有竞态；面板
  已打开时再次点击 chip（含同一会话）也会立即按该会话重查。
- **会话区「用量 / 余额」chip 的数值被省略号截尾**：配置单价后费用段「· ¥0.11」出现，
  两个 chip 合计宽度超出旧上限 189px 约两个字符，费用/余额的小数位被截断看不到。
  chip 行宽度上限放宽到 216px（容器查询 25cqw → 28cqw），实测两个 chip 完整显示、
  模型选择器与发送键不换行；超限时仍按省略号截断，完整值在悬浮提示里。

## v0.11.1 — 2026-09-14

### 优化

- **用量记录的单价与用量文案对齐 DeepSeek 官网叫法，并统一字段顺序为「缓存命中 → 缓存未命中 → 输出 → 缓存写入」**：
  - 单价字段改名：原「基准输入 / 基准缓存命中」改为「基准输入（缓存命中）/ 基准输入（缓存未命中）」，
    兜底单价、分时段价同步改名——之前「缓存命中」看不出是输入价，容易误填。
  - 全部 8 处展示位置统一调序：按模型配置单价、分时段价输入框、兜底单价、内置默认单价表、
    记录详情弹窗、KPI 汇总卡片、分组统计表（原笼统的「缓存」列拆成「输入(命中)/输入(未命中)」两列）、
    调用明细表（排序功能不变，列换位）；CSV 导出列序同步对齐。
  - 基准单价下新增一行说明：**缓存写入＝未命中缓存、本次新建缓存的那部分输入**（Claude 等按溢价
    单独收建缓存费），DeepSeek 不收此项、填 0 即可——解释官网三档价与四档单价字段的对应关系。
    （口径：输入未命中/缓存命中/缓存写入三数互斥，计费输入=三者之和，DeepSeek API 不上报缓存写入。）

### 修复

- **`test-tokenlog-host.mjs` 的时间耦合缺陷**：内置价 `deepseek-v4-flash` 带 9~14 时高峰分时价，
  用例用 `Date.now()` 做时间戳，跑在北京时间 9~14 点内时峰时价取代基准价、断言必失败
  （即上午跑测试会挂、下午就过）。改用「倒退 12 小时」的时间戳错开高峰时段，任何时间运行均稳定。

## v0.11.0 — 2026-09-10

### 新增

- **用量记录支持自定义模型单价，费用按自填单价计算并持久保存**：费用一直是用单价估算的，而
  之前只有「官网抓取刊例价 → 内置默认表 → 兜底价」三级，且抓取价优先级最高——官网刊例与实际
  计费（第三方中转、折扣、自建端点）常有出入，用户无法修正。现在用量记录页新增**独立的「单价设置」
  子弹窗**（可最大化 / 拖动 / 缩放）：可按模型逐条配置单价（人民币元 / 百万 tokens），并设置兜底单价、
  USD→CNY 汇率、是否继续自动同步官网价。配置写入宿主 settings（`dsh-dock.tokenlog`）跨浏览器/重启
  持久保存；计价优先级调整为 **自定义单价 > 官网同步价 > 内置价 > 兜底价**。费用改为在查询/导出时按
  当前单价即时重算，因此改价立即生效、无需重扫历史；KPI 卡、分组汇总、明细与 CSV 一并采用新价，
  明细中走兜底价的金额标注「兜底」、详情弹窗显示「计价来源」，便于补齐单价。
- **分时段价支持多段**：每个模型可配置任意多段时段价（如 DeepSeek 的高峰/优惠时段），命中哪段用哪段、
  时段外回落基准价；支持跨零点（如 `23~7`）与半点边界（如 `8.5` 表示 08:30），`start=end` 表示全天。
  旧版单段 `peak` 配置在读取时自动归一为多段，历史数据无需手工迁移。

### 修复

- **tokenlog 的 settings 覆盖配置此前从未生效**：旧实现读的是 `dsh-dock-tokenlog` 命名空间，
  但该命名空间从未注册——settings 服务的 `get()/mutate()` 只作用于已注册命名空间，因此
  「用 settings.yaml 覆盖汇率/单价」的说明与代码实际行为不符。现改为随插件一起注册的
  `dsh-dock.tokenlog` 段（`src/host-core.js` 的 `DockConfig`）。

### 安全

- 官网价目抓取地址（`pricingUrl`）在发起请求前做 SSRF 校验：仅允许 http/https，且拒绝
  localhost、环回、私有与保留地址。

### 测试

- 新增 `scripts/test-tokenlog-host.mjs`（并接入 `npm run test:host`）：覆盖自定义单价优先级、
  改价后查询即时重算、单价参数校验（空匹配 / `*` / 负值一律 4xx 且不落盘）、pricingUrl 出站校验，
  以及多段分时价（含跨零点、08:30 半点边界、旧单段 `peak` 兼容、非法时段 4xx）。
- 修正客户端视图回归测试的一处**假通过**：测试桩从未返回 `/dsh-dock/tokenlog/query` 响应，
  导致 `TokenLogView` 渲染即抛错、被错误边界降级为一行提示，而断言用的 marker「用量」恰好能在
  功能卡片的描述文字里命中，于是「渲染正常」一直是假的。现补上 query 桩数据，并把 tokenlog 的
  marker 换成只有真实视图才有的「调用明细」，另加明细表/单价入口/金额等断言。
- 客户端新增用例：子弹窗必须 `createPortal` 到 `document.body`（防面板祖先的
  `backdrop-filter`/transform 改变 `position:fixed` 包含块，导致子弹窗退化成「贴着弹层内定位」）。

## v0.10.3 — 2026-09-10

### 修复

- **切模型 / 打开历史会话卡住，报「模型操作失败」**（`preset "…" failed to mount`）：`dsh-dock`
  **agent 预设**（`~/.dsh/.agent-presets/dsh-dock/`，是 dsh 自带 `standard` 预设的副本）未跟上
  dsh `0.1.5-rc.1` 的 schema 变更——`@deepseek-ai/dsh-persona` 把 `config.text` 换成必填的
  `config.prefix` + `config.suffix`，导致预设挂载失败；因预设是 standing mount，**所有绑定该预设的
  会话都无法 resume**。已按新版 `standard` 对齐（persona 改 `prefix`/`suffix`，并补上 rc.1 新增的
  `dsh-tool-present` 行）。实机验证：用该预设建会话成功、对报错会话切模型成功。

### 变更

- README 截图引用整理：移除 1 张不再适用的示例图，保留 9 张（各自对应功能页）。

### 文档

- `docs/workflow.md` §6 新增「升级核对（dsh 换版本后必做）」：与新版 `standard` 结构化对比的方法、
  0.1.5-rc.1 已知两处漂移、以及**真机 mount 验证**步骤（并点明只查 `agentPresets/list` 不够——
  它不校验 config schema）。
- `docs/session-notes.md` 记录本次变更过程；README 常见问题补「切模型/会话卡住」一条。

## v0.10.2 — 2026-09-10

### 修复

- **切模型卡住 / 「模型操作失败」——dsh-dock agent 预设未跟上 dsh 0.1.5-rc.1 的 schema 漂移**
  （仅影响本地 `~/.dsh/.agent-presets/dsh-dock` 预设，不改插件代码）：dsh 升到 `0.1.5-rc.1` 后
  `@deepseek-ai/dsh-persona` 把 `config.text` 换成必填的 `config.prefix` + `config.suffix`，而本预设
  仍是旧写法 → 预设挂载失败（`$.prefix missing required value`）→ 因为预设是 standing mount，
  **所有绑定该预设的会话都无法 resume**：历史会话切模型、打开会话都卡住并弹
  `preset "dsh-dock" failed to mount`。已按新版 `standard` 对齐（persona 改 `prefix`/`suffix`，
  并补上 rc.1 新增的 `dsh-tool-present` 行）；实机验证：用该预设建会话成功、对报错会话切模型成功。

### 文档

- `docs/workflow.md` §6 新增「升级核对（dsh 换版本后必做）」：与新版 `standard` 结构化对比的方法、
  0.1.5-rc.1 已知两处漂移（persona 的 `prefix`/`suffix`、新增 `dsh-tool-present` 行）、以及
  **真机 mount 验证**步骤（并点明只查 `agentPresets/list` 不够——它不校验 config schema）。
- `docs/session-notes.md` 记录本次定位与教训；配套技能 `dsh-dock-release` 补「预设对齐」一节。

## v0.10.1 — 2026-09-10

### 文档

- **插件界面截图全部重拍**（`screenshots/`，10 张，统一 1280×720）：新增【模型余额】【任务通知】【运行状态】【趣味游戏】【设置 → 功能坞】五张；【用量记录】【模型设置】【任务动画】【会话区随身小控件】四张按 v0.10.0「任务通知 / 运行状态拆成独立菜单」之后的界面更新，页脚版本号同步刷新。任务通知页截图对其钉钉 / 飞书 Webhook 做了脱敏（显示为 `••••••（已脱敏）`），真实群机器人地址不进仓库。
- **README 界面章节补齐**：功能一览表补【趣味游戏】条目；把已过时的「手机接力（扫码接力）」更正为菜单项实际名称【远程访问】（描述也改为账号密码网关口径）；「各功能使用」下为用量记录 / 模型余额 / 任务通知 / 运行状态 / 趣味游戏补上对应截图，「启用 / 停用功能」补设置页截图（含文件内重复副本段，两处同步）。
- `docs/session-notes.md` 记录「模型列表 / 侧边栏加载慢」的定位结论：是**宿主进程内 dsh 自身的空转**（`dsh-agent-presets` 反复扫描 preset 目录 + `commands/change` 反馈回路），与 dsh-dock 无关——停用插件全部功能、乃至移走 `dsh-dock` agent preset 后循环依旧，新起的 dsh 实例（0.1.5-rc.1）均无此现象；插件用到的宿主 API（`llm.listConfigurableProviders` / `resolveModelInfo` / `prepareCall` / `BlockAssembler`、`credentials.credentialRef` 等）在 rc.1 中均存在。

## v0.10.0 — 2026-09-09

### 新增

- **【运行状态】独立成功能模块**（`features/runstate/`，宿主 + 客户端两半，功能坞左侧菜单独立项，order 150）：原「任务动画」页里的运行状态区块整体拆出——进行中任务（阶段 / 耗时 / 回合 / 步骤 / 工具 / Token）、等待确认横幅（含等待的工具与说明）与最近完成（最多 10 条）。**只读模块**：数据取自宿主侧共享任务追踪，没有自己的 settings 配置段、不写盘；启停由功能坞的 `features.runstate` 开关负责。与「任务动画」「任务通知」各自独立启停：三个都开也只有一份会话事件订阅，全部停用立刻退订。
- **【任务通知】独立成功能模块**（`features/notify/`，宿主 + 客户端两半，功能坞左侧菜单独立项）：原「任务动画」页里的通知部分整体拆出——通知事件（完成 / 异常 / 需确认）、提醒方式（卡片停留时长 / 浏览器系统通知 / 6 种提示音试听）、钉钉推送、飞书推送。与「任务动画」**互相独立、各自启停**：关掉动画只留通知、或只留动画都能正常工作。配置存在 settings 的 `notify` 段，升级时自动从旧的 `animation` 段迁移（含 Webhook，迁移一次后置标记不再写盘）。
- **宿主侧任务追踪抽成共享模块** `src/task-track.js`：会话级追踪（开始 / 结束 / 回合 / 步骤 / 工具 / Token / 阶段 / 待确认项）用引用计数共享一份实例——多个功能都开也只订阅一次会话事件，全都关立刻退订；新增 `onFinish` 订阅供通知模块做群机器人推送。
- **宿主半部回归测试** `scripts/test-task-host.mjs`（`npm run test:host`，无外部依赖）：断言共享追踪只有一份订阅且逐个停用后正确退订、`/dsh-dock/animation` 与 `/dsh-dock/notify` 各写自己的配置段互不污染、`/dsh-dock/runstate` 只读（无 config 方法、不写 settings）、Webhook 校验与测试消息的 400 提示、任务结束时钉钉推送按 `notifyOnComplete/notifyOnError` 筛选、以及一次性迁移的字段与幂等性。

### 变更

- **任务动画页只剩动画**：运行动画开关 + 19 种模式 + 桌面伙伴大小；运行状态（进行中任务 / 等待确认 / 最近完成）已独立成左侧「运行状态」菜单，通知见「任务通知」。提示音 / 通知卡片 / 系统通知 / 钉钉飞书相关代码与样式全部移入 `features/notify/`（`dkan-toast*`、`dkan-sound*`、`dkan-row*` 等类名随模块改为 `dknt-` 前缀，各模块样式自洽、可单独提取发布）。
- **任务通知页只剩通知**：通知事件 / 提醒方式 / 钉钉推送 / 飞书推送四组设置；原有的运行状态区块移除（同一份数据现在由「运行状态」页专管），相关代码与样式（`dkan-task*` / `dkan-tag*` / `dknt-task*` / `dknt-tag*` / `dknt-refresh`）一并清掉，新模块用 `dkrs-` 前缀自成一套。
- **首页总览概要各归其位**：【运行状态】卡片显示「N 个任务进行中 / ✋ N 项等待确认 / 空闲 · 最近完成…」；【任务动画】卡片改显示当前动效模式与开关状态（桌面伙伴另报大小）；【任务通知】卡片改显示通知事件 + 提醒方式 + 已开启的推送通道。
- `src/host-core.js` 的 `DockConfig.animation` 只保留动画字段，新增 `notify` 段（含 `migratedFromAnimation` 标记）与 `NOTIFY_FIELDS` / `migrateNotifyConfig()`；运行状态模块无配置段。
- `scripts/extract-feature.mjs` 一并复制 `src/task-track.js`，并修掉三处会让「提取独立包」中途失败的老问题：生成的客户端入口硬编码 `view.js`（.jsx 模块直接失效）、`scripts/` 目录没创建就写构建脚本（ENOENT）、生成 package.json 时引用了脚本里并不存在的 `feature` 变量（ReferenceError）。现已实测 `notify`（.jsx）与 `balance`（.js）两个模块都能完整提取。
- 功能开关沿用 v0.9.5 的「默认关闭、按需开启」：新菜单项「任务通知」「运行状态」默认停用，需在功能坞里打开。

### 修复

- **【模型设置】开关永远开不起来 → 目录接口整片 404**（v0.9.5 起存在）：宿主功能 id 叫 `models`、客户端视图 id 叫 `modelconfig`——面板开关与启动补推都按客户端 id `POST /dsh-dock/features`，宿主注册表里没有这个 id，一律 404「未知功能」（客户端静默吞掉），宿主半部从不 setup，`/dsh-dock/models` 从未注册；v0.9.5「默认全关」之前宿主默认开着才没暴露。现宿主 id 改齐为 `modelconfig`（回归「目录名 = 视图 id = 宿主 id」约定，新增双半部 id 一致性测试锁死），启动时一次性迁移开关表旧键 `features.models` → `features.modelconfig`（幂等，新键已存在不覆盖），读取侧在迁移落盘前做兼容别名兜底；开关补推失败改为 `console.warn`（fetch 对 404 是正常 resolve，`.catch` 接不住 HTTP 错误——本次排查半个版本才定位就是因为无声）。
- **Windows 下 `npm run build:client` 直接失败**：npx 缓存目录写死 `/tmp/npm-cache`（POSIX 路径），Windows 下 npx 解析不到 esbuild。改为系统临时目录，且 esbuild 来源支持三级解析：`DSH_DOCK_ESBUILD` 环境变量显式指定 → 仓库本地安装（`node_modules/esbuild`）→ npx 兜底。
- **新功能「面板显示已启用、宿主却从未 setup」→ 路由整片 404**（升级后第一次开【任务通知】必踩）：点开关时宿主进程还是旧版本，POST `/dsh-dock/features` 打了 404，开关只落在浏览器本地；宿主重启后其持久化表里没有 `notify`，宿主便从不 setup 该功能，`/dsh-dock/notify/*` 全 404，而面板把 404 一律说成「宿主进程是旧版本」，看着像没重启。现在 `initFeatureState` 拉宿主开关表时，若某功能**宿主从未记录过**（`persisted` 里没有它），就补推一次本浏览器的选择；宿主已显式记过 `false` 的不覆盖（多设备场景不能拿旧值去顶）。
- **404/405 提示不再误报**：点明两种可能（宿主进程为旧版本 / 该功能在宿主侧未启用）并给出可操作办法（重启 `dsh web`，或把功能坞里的开关关掉再打开）；任务动画页、任务通知页与运行状态页同步改。

## v0.9.9 — 2026-09-09

### 修复

- **点「任务动画」整个面板界面消失**（v0.9.3 起存在）：`features/animation/view.jsx` 的「等待确认」计数 `waitingCount` 误声明在 `if (!cfg) { … } else { … }` 的 else 块内，却在块外的「运行状态」区块引用 → 渲染期抛 `ReferenceError: waitingCount is not defined`。宿主把每个插槽条目包在 SlotErrorBoundary 里，条目渲染抛错即被换成空 div——表现为弹层/设置页「界面整个没了」，而侧栏「功能坞」入口仍是选中态。现把该计数提到函数体作用域（两条渲染路径共用），任务动画页恢复正常。
- **内置视图也包错误边界**（`src/shared.js` + `src/client.jsx`）：此前只有外部包视图包 `FeatureBoundary`，内置视图裸渲染——任何内置视图渲染抛错都会连带打没整块面板。现在视图、首页概要（HomeStat）、会话区小控件（Chip）、全局浮层（Overlay）全部包边界，并带 `key`（切换功能时重建，避免错误态串页）与 `label`（提示里点明是哪个功能）；错误同时 `console.error` 输出，降级提示为「「功能名」渲染出错：…（已隔离，不影响面板其他功能）」。

### 新增

- **内置视图渲染回归测试** `scripts/test-client-views.mjs`（`npm run test:client`，无外部依赖，~130ms）：在 vm 沙箱里加载构建产物 `client.js`（与浏览器跑的同一份代码），用极简 React 替身逐个启用 8 个内置功能渲染设置页面板，断言「渲染不抛错 + 出现该功能实际内容」（任务动画还跑带状态的多趟渲染，覆盖拉取配置后的分支），并额外断言错误边界隔离行为。该脚本就是本次定位问题的反馈回路——它先以 `waitingCount is not defined` 变红，修复后转绿。

## v0.9.8 — 2026-09-08

### 修复

- **用量记录启动不再被旧会话刷屏**：历史扫描遇到旧版(v0→v1 迁移)、损坏或缺 seq 等无法解码的会话，原本每条打一行 `console.error`——积累几十个旧会话后一启动终端就被刷满。现改为仅统计失败总数并汇总成一行（最多列前 3 条样例明细），其余静默跳过；这类读取失败本是常态，不影响已可解码会话的统计。

## v0.9.7 — 2026-09-07

修复 v0.9.5 引入的严重回归：功能默认关闭后**宿主侧半部从未启用**，用量记录、模型余额等所有依赖宿主路由的功能全部 404。

### 修复

- **宿主侧功能开关与面板彻底打通**：v0.9.5 把所有功能 `defaultEnabled` 翻转为 false，但宿主侧路由注册只认 `defaultEnabled`——浏览器面板里开开关只写 localStorage（仅影响 UI），宿主半部的路由从未注册，导致用量记录/模型余额/图片理解代理等所有 Host 路由 404。
- 现在开关持久化到 settings（`dsh-dock.features`，id → boolean）：浏览器面板 toggle 即时 POST 到新路由 `/dsh-dock/features`，宿主半部即时 setup/dispose；重启 dsh 后按持久化表恢复宿主侧功能（初始读取放在 settings 注册回调里——apply() 同步执行时命名空间尚未生效）。
- 客户端 `initFeatureState` 启动时从 `/dsh-dock/features` 拉取宿主持久化开关：跨浏览器/换电脑后默认值不再各说各话（本浏览器 localStorage 已表态的 id 仍保持本地值）；宿主旧版本无此路由时静默降级。
- `/dsh-dock/features` GET 返回各功能宿主侧状态（enabled/error）与持久化表，POST `{ id, enabled }` 同步开关。
- **远程访问网关上游会话自动兑换**（恢复 npm 0.9.6 已发布但未入 git 的改动）：网关经 `connection.authenticatedUrl` 服务端兑换 dsh web 启动令牌会话，代理请求（含 WebSocket 升级）自动附带上游 Cookie——远程浏览器只需网关账号密码，不再需要手动访问令牌地址；上游 401 自动重兑。远程访问宿主半部 `defaultEnabled` 回到 `true`（网关路由/账号服务随插件加载，部署依赖）；注意 v0.9.6 起面板开关会同步到宿主，显式停用远程访问会连网关一起关掉。

## v0.9.5 — 2026-09-04

功能坞所有功能默认关闭、按需开启（用户反馈：默认不应全开）。

### 变更

- **功能默认开关语义翻转**（`src/shared.js`）：从「未声明 `defaultEnabled` 即启用」（`!== false`）改为「仅显式声明 `defaultEnabled: true` 才启用」（`=== true`）——新增功能忘写开关字段时不再意外默认开启。
- **10 个功能模块全部显式声明 `defaultEnabled: false`**：任务动画、模型余额、用量记录、图片理解代理、远程访问、趣味游戏、模型配置（原显式 `true` 翻转），主题信息、心跳监视（原先无字段靠旧兜底默认开，补显式声明）。

### 边界

- **已手动开/关过的功能不受影响**：开关以 localStorage（`dsh-dock/features/v1`）持久化值优先，只有从未动过开关的功能落到现在的新默认（关闭）。
- **手机抽屉把手随之默认不出现**：它挂在「远程访问」功能的 Overlay 上、同样受开关门控——面板里开启「远程访问」后才出现。
- 外部经 dockBridge 注册的独立功能包行为不变（惰性建项仍默认启用）。

## v0.9.4 — 2026-09-04

手机端会话抽屉把手：修复远程访问手机端侧边栏「只收不放」——看不到会话列表，也没有任何入口能把抽屉拉出来。

### 修复

- **手机端侧边栏只收不放**：DSH 窄屏（≤700px）下侧栏显隐由 layout store 的 `narrowExpanded` 驱动，原生「打开侧边栏」按钮渲染在侧栏窄轨里；而 v0.8 引入的手机端适配把布局栅格压成 `0px minmax(0,1fr) 0px`，窄轨连同里面唯一的展开按钮一起被裁掉——手机端默认收起后从此无路可开。

### 新增

- **抽屉把手**：贴左边缘的窄把手（30×48、右圆角、玻璃底，与趣味游戏浮标同款边缘吸附语言），汉堡线图标明确「拉出抽屉」语义。位置动态放在趣味游戏浮标正上方（浮标太靠上会压顶栏标题时改放其下方；浮标被拖走/隐藏时兜底左侧中部 38% 视高），随浮标拖拽与窗口缩放跟随。点击转发原生「打开侧边栏」toggle，会话列表以浮层抽屉展开。
- **抽屉遮罩**：抽屉展开时背后压暗（z 序：把手 70 < 遮罩 75 < 侧栏 80 < overlayLayer 90），点遮罩、抽屉外区域、或选中会话 300ms 后自动收起。收起统一走全局捕获 handler 单一路径（遮罩自身不挂点击），避免同一事件两次 toggle 把抽屉又顶开。
- **显隐同步**：MutationObserver 监听 `data-sidebar-collapsed` 属性翻转即时响应 + 1.5s 低速轮询兜底（覆盖路由重渲）+ resize 复位。把手只在窄屏、侧栏收起、无设置弹窗时出现，桌面宽屏零影响（媒体查询 + JS 双重门控）。

### 机制

- **双版实现、先到先得互斥**：客户端 Overlay 版（`MobileRelayOverlay`）随 client bundle 分发——bundle 的 rev 按内容 hash 实时计算，**浏览器刷新即生效，无需重启 `dsh web`**；宿主 tapIndex 注入版（`features/mobile-relay/host.js`）在进程启动时注册，重启后于 `<head>` 先执行并设 `window.__dshDockMobileDrawer` 接管，客户端版看到旗标自动让位。两版样式与行为保持一致。
- **测试**：host 单测新增注入断言（把手/遮罩/UUID 兜底 + 幂等）；手写最小 DOM 仿真跑通窄屏全交互（收起显把手 → 点击展开 → 外部点击收起 → 选会话自动收起）与宽屏零副作用；网关单测无回归。

## v0.9.3 — 2026-09-03

任务动画「需确认提醒」：工具等待用户批准时发通知 + 声音提醒；等待/卡住卡片按会话隔离并挪出正文区；两处卡顿优化。

### 新增

- **需确认提醒**：dsh 任务的工具需要用户批准（如沙箱越界执行命令）时，之前只有会话里的审批面板，人走开了完全不知道任务卡在等你。现在宿主半部从会话事件流（`approval/asked` → `approval/decided`）追踪每个任务的待确认项并经 `/status` 下发，浏览器浮层检测到即：弹右上角通知卡片（琥珀描边 + 呼吸光晕，**常驻到用户处理**，正文含任务名、工具名中文场景如"执行命令/修改文件"、确认说明）；播放专门的**确认音**（每种音效新增一套上扬双音，与完成音/异常音听感区分）；页面在后台且开启系统通知时同步推送。同一会话同一确认项只提醒一次，不重复打扰。「完成通知」区新增「需确认提醒」独立开关（默认开）。
- **状态可视化**：等待确认时右下角徽标与桌面伙伴文字变为「等待确认」（琥珀色），面板「运行状态」副标题显示「N 项等待确认」，任务行打「等待确认」标签并列出等待的工具与说明。

### 修复

- **等待/卡住卡片跨会话误显**：「编程等待中」「等待确认」卡片此前取全局最早的卡住任务——别的会话在等确认，新开的会话屏幕中间也会弹出来。现在浮层从 dsh 侧栏读取当前高亮会话（`role="treeitem"` + `aria-selected`），只对当前正在看的会话显示这两类卡片，切走即消失。
- **卡片挡正文**：卡片从屏幕正中移到右上角（与通知卡片同侧、错开高度）；窄屏（≤760px）回落顶部居中避免溢出。
- **待确认项漏提醒**：宿主 `/status` 下发的 approvals 未带 id，客户端按 id 去重会把同一任务后续新确认项全部误判为已提醒。现下发 `{ id, toolName, reason, askedAt }`。

### 性能

- **停靠点校准轮询降频**：星际模式输入框校准此前每 700ms 永久执行一次全页 `querySelectorAll` + `getBoundingClientRect`（每次强制布局），空闲时也持续掉帧。现跟随任务活动：有活动 700ms，空闲 20 秒后降至 5 秒，任务恢复立即回快节奏。
- **当前会话识别限频**：新增的侧栏会话标题 DOM 查询做 1 秒限频，不随 850ms 轮询逐次执行。

## v0.9.2 — 2026-09-02

趣味游戏四连修 + 远程访问选目录的 Windows 跨盘改进。

### 修复

- **中国象棋四边边线缺失**：棋盘外框是 SVG `<rect>`，但样式只给 `line/path` 设置了描边色，`rect` 一直无色——整个外框（连同四条边线）渲染不出来。现在外框恢复显示并保持加粗。
- **中国象棋走棋无动画、看不清对方走了什么**：此前落子是瞬间换位。现在玩家与 AI 走子都有"跳跃飞行动画"（起跳抬升 → 落地回弹，吃子时被吃棋子原地缩小淡出，动画期间输入锁定）；AI 思考时间 90ms → 420ms 更易看清；棋盘下方新增传统棋谱行（如"炮八进一 / 炮2进7"，红方中文数字、黑方阿拉伯数字，各从己方右侧数起），起点虚线圈 + 终点红点标记上一手；撤销/重开同步清理棋谱。
- **俄罗斯方块背景格子不明显**：网格线原来只有 12% 透明度细线，空格几乎与背景融为一体。改为经典机台风格——每个空格画深色内嵌圆角"凹槽坑"（微描边），网格线提亮加粗（28% / 3px），彩色方块抬到网格层之上不受凹槽压暗。
- **打砖块按空格不发球**：`launchBall()` 把"已发球"标志写在游戏对象上（`g.launched`），主循环却读球对象（`b.launched`，恒 undefined）——每帧把球按回挡板，永远发不出。统一读 `g.launched`；顺手支持点击/触摸画布发球。
- **极速赛车撞车无反应**：玩家车对象初始化漏了 `w`/`h` 字段，碰撞检测 `overlap()` 里 `a.x + a.w` 为 NaN、比较恒 false，怎么撞都不结束。补全字段后同车道碰撞正确判负，相邻车道不受影响。

### 新增

- **远程访问（Windows）选工作区可跨盘**：开启远程访问后目录选择器切官方浏览模式，但该后端从主目录起步、面包屑折叠在主目录之内——Windows 上永远看不到 `C:\` 之外的磁盘。现在宿主半部在运行时包装 browse 能力：主目录层级注入全部盘符入口、任意盘根目录注入其他盘符入口（`stat` 并发探 A–Z，结果缓存 5 秒，探测失败保持原列表），像资源管理器一样跨盘选工作区；仅主目录与盘根两级注入，非 Windows / 原生对话框模式零改动。

## v0.9.1 — 2026-09-02

亮色主题全量适配：此前引用了宿主不存在的变量名（`--dsw-alias-accent`、`--dsw-alias-state-warning-primary`），强调色/警告色全部落到暗色系回退值，亮色白底下看不清甚至看不见。

### 修复

- **主题桥接层**（外壳样式头部，`body` / `body[data-ds-dark-theme]` 两套取值）：新增 `--dk-accent`（亮深蓝 #2f6fed / 暗亮蓝 #4d9fff）、`--dk-warn`（亮深琥珀 / 暗亮琥珀）、`--dk-ok/--dk-err/--dk-tdim/--dk-hover-tint`，并补齐 `--dsw-alias-accent` 等宿主缺失别名的自动映射。全局 42 处强调色、5 处拼错的 warning 变量统一改走桥接层（外壳 / tokenlog / balance / modelconfig / animation）。
- **弹窗遮罩白上白**：遮罩原用 `bg-layer-1`（亮色=纯白）半透明，弹窗与页面完全无层次；改为固定深色半透明，两主题通用。
- **模型余额**：Provider 调色板从 300–400 级浅色整体换为中深色（可作亮色文字色），名称/模型 chips/赠送充值小字白底可读。
- **用量记录**：表格分隔线与行悬停色改主题变量；会话用量 chip 圆点接 `--dk-warn`。
- **任务动画**：阶段色、火花/彩带粒子色、编码卡警示文字、货运飞船标签色全部换深色系；通知标记色接桥接变量。
- **趣味游戏**：面板页强调色接 `--dk-games-accent`（亮深紫 / 暗亮紫）；反应堆通关提示文字由浅绿改主题成功色。游戏浮窗自持的暗色星空主题保持不变（深色底浅字，两主题一致）。
- **远程访问**：入口强调色接桥接变量（原硬编码 #38bdf8 亮色下过浅），主按钮文字改白色，安全说明框琥珀色接 `--dk-warn`。

## v0.9.0 — 2026-09-02

远程访问的手机端体验：修复设置/模型页在手机上完全不可用的问题，并把官方界面的窄屏排版补齐。

### 修复

- **手机端设置/模型页报 "settings are unavailable in this browser"**：DSH 客户端按"页面主机名是否回环"决定设置镜像模式——手机经网关访问的是局域网 IP，客户端自判非回环，设置镜像被构造为永不读档的 memory 模式（根本不发请求）。而服务端信任栏只看 Host/Origin，经网关（登录后）的请求早已映射为回环信任、特权接口实际全部放行——拦住手机用户的只剩客户端这一处自我判定。网关现对连接客户端 bundle 做定点改写（判定恒真；按 rev 缓存，上游升级改了片段则原样透传退化为官方行为）。全客户端只有设置镜像消费该标志，无其他副作用。
- **功能坞面板被收起侧边栏（图标栏）遮挡**：侧边栏浮层化后与面板同处一个层叠上下文链，图标栏（z 80）压住了挂在 overlayLayer（z 20）里的面板。窄屏下把 overlayLayer 提到 z 90，面板（z 200）恢复正常全屏置顶。
- **代理卫生**：浏览器侧中途断开时把上游响应读完丢弃而非 RST（宿主对半路 RST 敏感，实测会被打崩整进程）；改写 bundle 后重设 content-length 并去掉上游的 transfer-encoding（两种长度语义并存是协议错误）。
- **宿主进程健壮性护栏**：Node 对已半脱离 HTTP 管理的 socket 上到达的传输层重置没有兜底监听——手机锁屏/切后台、页面跳转中断请求等日常 RST 会以未捕获异常打崩整个 dsh web（实测复现两次）。插件加载时安装窄护栏：只吞 ECONNRESET/EPIPE 纯传输层错误（等价 nginx 对客户端断连的处理），其余未捕获异常照常抛出。

### 新增

- **移动端排版补丁**（随页面注入，≤700px 媒体查询护栏，桌面零影响）：官方设置弹窗在窄屏是"左导航+右内容"两栏，内容栏被压到不足百像素（一字一行）——改为纵向全屏、导航横排；侧边栏展开从栅格挤占会话区（窄屏会话区只剩 ~110px）改为浮层覆盖；详情栏（工具行详情）在收合态也强制可见、顶替会话区（用户只看到"点击消息流中的工具行查看详情"占位而没有输入框）——改为仅在展开时右侧浮层，栅格轨道窄屏下归零、会话区恒满宽。选择器按语义后缀匹配（哈希前缀随版本变化，后缀稳定），DSH 升级改结构时自然失效不影响其他功能。
- **移动端行为补丁**：侧边栏浮层化后，点历史会话/新会话后自动收起侧栏并展示会话内容；点侧栏外的页面区域也收起（浮层语义）。侧栏内的其他操作（工作区折叠、搜索、功能坞入口）保持原生行为。
- 趣味游戏悬浮球窄屏下缩小并半透明（悬停恢复），不再大面积遮挡正文。

#### v0.8.2 增量（与 v0.8.1 合并随本版首发）

修复远程访问（v0.8.1）首发的三个问题：登录表单 400、开启后主实例 /api 整面 404（会话/设置/模型页全不可用）、网关默认端口与主端口脱节。

### 修复

- **登录表单提交报「请求格式错误」**：登录页是原生 `<form>` POST（urlencoded），网关却一律按 JSON 解析，浏览器登录必然 400。现按 Content-Type 分流：表单流成功 303 回首页、失败 303 回错误态登录页（`?e=1`）；JSON 流（面板/脚本）维持原 JSON 响应。
- **开启远程访问后主实例大面积不可用**：浏览模式补丁把 browse 行平铺写进补丁层，而 DSH 补丁语义对未知 id 的普通行整行跳过（`directory-picker` auto 行又被停用），`directoryPicker` 服务无人提供 → apiProxy 永久 pending → **全部 /api RPC 404**（会话列表、设置、模型页、凭据全失效）；带此补丁重启则直接起不来（"1 entry did not activate"）。现按 DSH 语法包进 `insert:` 块写入（临时实例 A/B 验证：boot 正常、核心 RPC 全 200、browse 前后端正确挂载）；插件加载时自动把旧坏形态无损改写（自愈），不再需要手工改补丁文件。
- **网关默认端口跟随主实例（主端口+1）**：网关和主服务是同一台机器上的两个监听，结构上不能同端口；此前固定 3081 与用户主端口无关联、令人困惑。现留空默认主端口+1（3080 → 3081），面板文案说明两个端口各自的用途。

#### v0.8.1 增量（与 v0.9.0 合并随本版首发）

【手机接力】重构为**远程访问（账号登录网关）**：主实例永远只监听 127.0.0.1（结构上不存在免登录的远程路径），局域网/虚拟网设备统一经 3081 端口的认证网关访问——未登录跳转登录页，账号密码登录后使用与本机完全一致的 DSH（同一实例，会话、任务进度、设置实时一致）。支持退出登录、改密全端下线、登录失败限速。此前"主实例+子实例"与"服务器模式 0.0.0.0"方案均被取代：前者任务状态跨进程不同步，后者把无认证的 DSH 裸露给局域网。

### 新增

- **账号密码登录**：面板设置账号与密码（密码只存加盐哈希，不明文落盘）；远程设备访问网关地址即见登录页（深浅色自适应），登录后发 HttpOnly 会话 Cookie（7 天滑动过期，免重复登录）。
- **退出登录**：远程设备在功能坞「远程访问」页点「退出登录」即可注销当前会话。
- **修改账号密码**：改后所有已登录设备立即作废、强制重新登录。
- **登录限速**：同一来源 IP 10 分钟内最多 10 次失败尝试。
- **工作区目录浏览模式**：开启远程访问时自动把目录选择器钉为浏览模式（补丁写入用户 profile 层，重启生效），远程设备可在浏览器里浏览/新建任意盘符目录（顶部面包屑/铅笔支持直接输入路径，如 `F:\workspace\demo`）。
- **测试**：网关单测（登录/退出/Host 改写/Cookie 透传/WebSocket 管道/兼容注入）与宿主单测（凭据流、补丁往返与幂等、旧服务器模式迁移）全绿；3480 实例实测登录全链路（302 → 登录页 → 401/200 → 会话代理 → 退出后 302）。

### 修复

- **非安全上下文 `crypto.randomUUID` 缺失导致局域网浏览器全面瘫痪**（会话列表空、设置目录加载失败）：局域网 `http://<IP>` 不是安全上下文，部分浏览器（老内核/加固内核）缺失该 API 而 DSH 客户端直接调用。经网关向 index.html 注入 ES5 兜底（实例属性 + `Crypto.prototype` 双路径补齐，`crypto` 被冻结的极端浏览器则整体替换该对象）；原生存在时绝不覆盖。若仍报错请强刷（Ctrl+Shift+R）排除旧页缓存。

### 说明

- **迁移**：若开启过旧「服务器模式」（0.0.0.0 直开），在面板点「开启远程访问」会自动移除该配置并提示重启——账号架构下主实例必须回到仅本机，否则存在绕过登录的直连路径。
- profile 里的插件是 `file:` 本地快照：同版本号 `pnpm add` 会跳过，更新代码后需 `pnpm remove dsh-dock && pnpm add file:<仓库路径>`。

## v0.8.0 — 2026-08-26

放开【趣味游戏】菜单：十个经典机台小游戏（星际躲避、反应堆点亮、俄罗斯方块、贪吃蛇、推箱子、打砖块、极速赛车、坦克大战、五子棋、中国象棋）随面板一起发布，全部为纯客户端玩法，不消耗 Token；并修复此前预览时的键盘、显示与棋盘问题。

### 新增

- **趣味游戏模块**（`features/games/`）：菜单放开，任务区 FAB 快捷入口与浮动游戏窗可用。十款小游戏全部本地运行（React / Canvas 渲染，无 LLM 调用、不写会话、不改工作区），默认不消耗 Token；游戏窗支持最小化、展开选择层、拖拽与缩放。
- **全局键盘控制**（`features/games/games/shared.jsx`）：所有游戏共用的 `useGameControls` 钩子——舞台打开自动聚焦，方向键 / WASD / 空格 / Enter 立即可用；点击舞台或从最小化恢复时重新夺回焦点；输入框内按键不受干扰；暂停 / 最小化时不抢焦点。窗口级监听与舞台原生按键去重，避免双重触发。

### 修复

- **修复键盘方向键失效**：此前仅靠舞台元素自动聚焦，焦点一旦落到按钮 / 弹层即失灵；现改为 window 级按键监听 + 点击夺焦，任何情况下方向键都可玩。
- **修复棋盘与落子不可见**：五子棋 / 中国象棋棋盘此前 `background` 简写被后续 `background-image` 覆盖、底层木纹色丢失，网格线盖满整盘，且石头 class 与 CSS 不匹配（`b/w` 无对应样式）导致落子看不见。现把多层背景合并为单条 `background:` 逗号列表，石头样式修正为 `black/white`，棋子与落点正常显示。
- **修复游戏显示不全 / 横向溢出**：窄窗口下推箱子、五子棋、象棋等固定像素宽度会超出容器；现棋盘改为 `min(100%, min(90vw, 320px))`，推箱子宽度改用 CSS `min()`，浮窗最大化时各游戏舞台 / 棋盘 / 控制条统一拉伸到满宽，不再被截断。
- **修复游戏结束后 R 键重开失效**：暂停判定误把「游戏结束」也计入，重开键被吞；现已只按窗口暂停状态监听，R 键在结束后可随时重开。
- **修复反应堆点亮崩溃**：恢复被误删的 `solved` 状态（此前渲染即 ReferenceError）。
- **FAB 样式修正**：快捷入口按钮 className 由 `dgfab-<side>` 改为 `dgfab-side-<side>`，与样式表一致，浮动入口正常贴边显示。

### 优化（2026-08-27 打磨）

- **游戏音效**：新增 WebAudio 合成音效引擎（零素材文件、惰性初始化），十款游戏全部接入——移动、旋转、落定、消行、吃食、落子、吃子、将军、反弹、碎砖、推箱、过关与胜负等场景均有反馈音。
- **棋盘正规化**：五子棋 / 中国象棋棋盘改用 SVG 精确绘制——外框加粗、格线与落子交点严格对齐；象棋带楚河汉界（竖线河界断开）、九宫斜线、炮兵位直角标，五子棋带星位；底板换木纹质感。
- **游戏浮窗自适应**：窗口高度随内容自适应（不再裁切一半画面）、无滚动条；棋盘按屏幕可用高度等比缩放，放大缩小不走样。
- **象棋规则与 AI 修复**：修复帅无步可走（走法生成误用车类型）与初始布局错位（黑卒 / 红炮行数归位）；记录局面指纹禁止重复将军死循环，AI 近似并列着法随机化不再呆板循环。
- **俄罗斯方块**：下键改为按住快速下滑（松开即停、仅对当前方块生效）；网格与方块严格对齐；修复「下一个」预览与实际出块不一致；窄窗布局不再遮挡。
- **贪吃蛇**：回车开始 / 暂停、结束后可重开；新增三档可调速度（默认慢速并本地记忆）。
- **推箱子**：关卡由 6 关扩至 15 关（BFS 求解器逐关验证可解、箱子与目标数一致）。
- **五子棋**：五连棋子高亮脉冲 + 胜负结果弹层动画；对局结束后可撤销续弈。
- **打砖块**：修复主循环帧函数遮蔽渲染函数导致的首帧崩溃（此前打开即无响应）。
- **选择层封面重绘**：五子棋 / 象棋 / 推箱子 / 坦克 / 俄罗斯方块封面改为与真实游戏一致的迷你画面。

### 说明

- 游戏默认不消耗 Token：全部玩法在浏览器本地完成，无任何 LLM / 宿主 API 调用（唯一网络请求是任务动画状态轮询，与游戏无关）。
- DOCK_VERSION 升至 0.8.0。

## v0.7.1 — 2026-08-26

功能坞移动工作台与交互优化，任务动画与金额展示升级。（手机接力、趣味游戏两项功能仍在打磨，将在下个版本发布，本版不含。）

> npm 包以 0.7.1 发布（此前曾误发并废弃 0.7.0，本版改用 0.7.1）。

### 改进

- **功能坞移动工作台**：窄屏（≤680px）切换为全屏移动工作台，适配顶部/底部安全区、横向功能导航、44px 操作目标并减少动态效果；桌面端的拖拽、缩放与分栏体验不变。
- **任务动画扩充到 19 种模式**：新增星际远征、星云潮汐、曲速航道、量子雷达、星座网络、萤火、深海脉动、棱镜、神经电路、引力波、灯笼等模式（叠加原 8 种），动画速度随任务活动（Token 速率）联动。
- **用量记录金额统一人民币**：界面/汇总/明细移除美元金额列，统一按汇率换算为人民币；会话区 chip 用紧凑人民币金额；CSV 导出金额列合并为 `costCny`，避免口径混乱。
- **模型余额统一人民币**：余额接口仅返回美元时按 7.2 汇率换算并标注「人民币（估算）」，与用量模块汇率口径一致。
- **会话区 chips 修复**：把余额/用量 chip 行宽度收窄为 `min(189px,25cqw)` 并收紧内距，修复长金额/长用量把右侧模型选择器/发送键挤到第二行、悬出输入卡圆角的问题（超限省略号截断，完整值在 title 悬浮提示）。
- 功能坞样式首帧延迟到浏览器空闲期注入，避免解析卡顿；切换左侧模块时滚动回到页首；DOCK_VERSION 升至 0.7.0。

## v0.6.0 — 2026-08-24

模型设置、图片理解代理与桌面伙伴三项核心体验升级；新增飞书任务推送，并修正会话界面与全屏动效的交互问题。

### 新增

- **飞书群机器人推送**：任务结束时可向飞书群机器人发送交互卡片；与钉钉并列配置，支持独立 Webhook、连通测试，并跟随「完成 / 异常」通知开关。
- **模型图片能力由用户声明**：模型设置中的「图片」勾选可直接将原图交给该模型；运行时探测仅作候选提示，不再擅自否定用户的已知能力。

### 改进

- **模型设置更及时、可读**：进入页面自动重新读取 Provider 目录，顶部新增刷新按钮；图片理解代理候选默认折叠，只显示已解析且声明图片能力的模型，并明确其未经联网实测的边界。
- **桌面伙伴重构为实时人物动作**：按推理、正文输出和工具调用的活动脉冲调节节奏；收紧为双工位，左侧检索、右侧编码。人物补齐头颈、肩肘腕与坐姿比例，椅子和身体始终面向桌面；思考时微抬头、单手托下巴，输出时键入，检索时转头扫视。
- **任务动效反馈增强**：呼吸光点增加阶段色、辉光和扩散光晕，频率随吞吐变化；主题切换可即时更新面板信息。

### 修复

- **修复会话输入区余额 / 用量 chips 在会话底部换行错位**：宿主输入卡 `.row` 是 `flex-wrap:wrap` 且换行优于收缩，chips 一长会整行变宽，把右侧模型选择器/发送键挤到第二行（左右错位）。此前仅把宽度上限设为 `min(280px,36cqw)` 仍偏宽——标准 780px 输入卡（行内容约 762px）下「add 钮 + 工作区选择器 + chips」与右侧「模型选择器 + 发送键」合计已超宽，即便 chips 未截断也会换行。本次把 chips 行上限收紧到 `min(189px,25cqw)`、内距收紧为 `padding 2px 6px`、`gap 2px`，实测余额 + 用量两 chip 完整显示、不再换行；超限仍用省略号截断（完整值在 title 悬浮提示），不会悬出输入卡圆角。
- 修复全屏星点、极光、火花和彩带等装饰子元素可能遮挡页面点击的问题。

## v0.5.0 — 2026-08-23

任务动画正式落地：8 种氛围动效 + 桌面伙伴 3D 场景、完成通知与提示音、钉钉群机器人推送、交互反馈层；功能开关持久化；余额页与开关交互全面美化。

### 新增

- **任务动画模块**（`features/animation/`，宿主 + 客户端两半）：
  - **运行动画**（任务进行中才出现，速度随 Token 吞吐自动加快）：流光细线 / 呼吸光点 / 轨道光环 / 环屏巡航 / 代码雨 / 星野（弯月 + 流星）/ 极光 / 桌面伙伴，共 8 种氛围模式，模式卡带缩微实时预览；
  - **桌面伙伴**：纯 CSS 3D 长方体拼装的具象人物（眉/眼/鼻/耳/头发/卫衣/坐姿腿脚鞋）坐镇四屏工位，流级实时同步任务阶段——思考仰头冒泡、输出低头敲键盘（中屏代码滚动）、查资料头部左右扫视；整卡可拖到屏幕任意位置（位置持久化），右下角手柄 / 面板预设（紧凑/默认/加大/特大）/ 滑块可缩放 85%~220%（自动保存），键盘方向键微调，适配 `prefers-reduced-motion`；
  - **完成通知**：右上角通知卡片栈（标题/模型/耗时/回合步骤/Token/摘要/结束原因），完成与异常分别开关、停留时长可选、浏览器系统通知（仅页面后台推送）；
  - **提示音**：6 种 WebAudio 合成音效（清脆双音/叮/金币/钟声/脉冲/琶音），完成与异常各配一音，可单独试听，无音频文件依赖；
  - **钉钉推送**：任务结束由宿主直发 markdown 到群机器人（浏览器关着也能推），Webhook 校验 + 连通测试，事件跟随完成/异常开关；
  - **交互反馈层**（与动画开关独立）：发送消息→纸飞机飞出、新会话→火花迸发、切换会话→流光掠过、切换工作目录→全屏微光、任务开始→底部光涌、任务完成→顶部彩带；
  - 新模块契约 **Overlay**：功能描述符可挂全局浮层，外壳统一渲染（dockBridge 外部功能同样支持）。
- **功能开关持久化**：每功能启停写入浏览器本地存储，刷新 / 重启后保持上次状态。
- **桌面伙伴大小可调**：面板页预设 + 滑块，浮层拖拽手柄，配置经宿主持久化。

### 改进

- **模型余额页美化**：余额改主数字卡（大号金额 + 赠送/充值分色小字，多币种横排），配额维度改进度条，模型 chips 默认折叠，Provider 按可查余额排序，连接信息弱化为单行小字。
- **开关交互统一**：状态与操作视觉分离（圆点 + 文字标识「运行中/已停用」，启停改 iOS 滑动开关），统一应用到首页卡片 / 弹层页脚 / 设置页 / 动画页 / 会话页小控件，补 `role=switch` 无障碍语义。
- **动效可见度加强**：代码雨 / 星野 / 极光加亮加浓，交互反馈元素放大带辉光，克制不干扰。
- 桌面伙伴场景精修：桌子缩短人物居中、显示器增至四台（扇形 + 竖屏叠放）、屏幕改整体向上滚动的无缝代码流、人物更拟人（圆角头身、脖颈、帽兜、桌上咖啡杯）。

### 文档

- README 增补任务动画完整使用说明（8 种动效 / 通知 / 提示音 / 钉钉推送 / 常见问题）与功能截图。
- 明确插件 token 消耗边界：默认零消耗；唯一主动调 AI 的图片理解代理默认关闭，模型设置页加黄色警示。

## v0.4.1 — 2026-08-22

- 修复：余额 chip 不随模型切换（改订阅会话级模型目录服务，选择器切换即时推送）。
- 修复：用量 RPC 405/404 降级为「宿主进程是旧版本，重启 dsh web 后重试」提示。
- npm 详情页公开化。

## v0.4.0 — 2026-08-22

- **模块化架构**：功能拆进 `features/` 独立模块（host.js 宿主半部 + view.js(x) 客户端视图），可单独提取成包发布，又可经 dockBridge 装回面板。
- **用量记录**：全部 LLM API 调用记账——秒级时间筛选、Token/费用统计（峰谷计价 + 官网价目自动同步）、分组汇总、明细检索与 CSV 导出。
- **会话区随身小控件**：余额 / 用量 chips 挂模型选择器左侧，点击定位功能坞对应页；每功能可设「会话页显示/隐藏」。
- 功能坞菜单次序重排；README 完善安装与使用说明。
