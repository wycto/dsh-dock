# 更新日志

本文件记录 dsh-dock 各版本的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)。

## v0.8.0 — 2026-08-26

放开【趣味游戏】菜单：十个经典机台小游戏（星际躲避、反应堆点亮、俄罗斯方块、贪吃蛇、推箱子、打砖块、极速赛车、坦克大战、五子棋、中国象棋）随面板一起发布，全部为纯客户端玩法，不消耗 Token；并修复此前预览时的键盘、显示与棋盘问题。

### 新增

- **趣味游戏模块**（`features/games/`）：菜单放开，任务区 FAB 快捷入口与浮动游戏窗可用。十款小游戏全部本地运行（React / Canvas 渲染，无 LLM 调用、不写会话、不改工作区），默认不消耗 Token；游戏窗支持最小化、展开选择层、拖拽与缩放。
- **全局键盘控制**（`features/games/games/shared.jsx`）：所有游戏共用的 `useGameControls` 钩子——舞台打开自动聚焦，方向键 / WASD / 空格 / Enter 立即可用；点击舞台或从最小化恢复时重新夺回焦点；输入框内按键不受干扰；暂停 / 最小化时不抢焦点。窗口级监听与舞台原生按键去重，避免双重触发。

### 修复

- **修复键盘方向键失效**：此前仅靠舞台元素自动聚焦，焦点一旦落到按钮 / 弹层即失灵；现改为 window 级按键监听 + 点击夺焦，任何情况下方向键都可玩。
- **修复棋盘与落子不可见**：五子棋 / 中国象棋棋盘此前 `background` 简写被后续 `background-image` 覆盖、底层木纹色丢失，网格线盖满整盘，且石头 class 与 CSS 不匹配（`b/w` 无对应样式）导致落子看不见。现把多层背景合并为单条 `background:` 逗号列表，石头样式修正为 `black/white`，棋子与落点正常显示。
- **修复游戏显示不全 / 横向溢出**：窄窗口下推箱子、五子棋、象棋等固定像素宽度会超出容器；现棋盘改为 `min(100%, min(90vw, 320px))`，推箱子宽度改用 CSS `min()`，浮窗最大化时各游戏舞台 / 棋盘 / 控制条统一拉伸到满宽，不再被截断。
- **修复游戏结束后 R 键重开失效**：暂停判定误把「游戏结束」也计入，重开键被吞；现已只按窗口暂停状态监听，R 键在结束后可随时重开。
- **修复反应堆点亮崩溃**：恢复被误删的 `solved` 状态（此前渲染即 ReferenceError）。
- **FAB 样式修正**：快捷入口按钮 className 由 `dgfab-<side>` 改为 `dgfab-side-<side>`，与样式表一致，浮动入口正常贴边显示。

### 说明

- 游戏默认不消耗 Token：全部玩法在浏览器本地完成，无任何 LLM / 宿主 API 调用（唯一网络请求是任务动画状态轮询，与游戏无关）。
- DOCK_VERSION 升至 0.8.0。

## v0.7.1 — 2026-08-26

功能坞移动工作台与交互优化，任务动画与金额展示升级。（手机接力、趣味游戏两项功能仍在打磨，将在下个版本发布，本版不含。）

> npm 包以 0.7.1 发布（此前曾误发并废弃 0.7.0，本版改用 0.7.1）。

### 改进

- **功能坞移动工作台**：窄屏（≤680px）切换为全屏移动工作台，适配顶部/底部安全区、横向功能导航、44px 操作目标并减少动态效果；桌面端的拖拽、缩放与分栏体验不变。
- **任务动画扩充到 19 种模式**：新增星际远征、星云潮汐、曲速航道、量子雷达、星座网络、萤火、深海脉动、棱镜、神经电路、引力波、灯笼等模式（叠加原 8 种），动画速度随任务活动（Token 速率）联动。
- **用量记录金额统一人民币**：界面/汇总/明细移除美元金额列，统一按汇率换算为人民币；会话区 chip 用紧凑人民币金额；CSV 导出金额列合并为 `costCny`，避免口径混乱。
- **模型余额统一人民币**：余额接口仅返回美元时按 7.2 汇率换算并标注「人民币（估算）」，与用量模块汇率口径一致。
- **会话区 chips 修复**：把余额/用量 chip 行宽度收窄为 `min(189px,25cqw)` 并收紧内距，修复长金额/长用量把右侧模型选择器/发送键挤到第二行、悬出输入卡圆角的问题（超限省略号截断，完整值在 title 悬浮提示）。
- 功能坞样式首帧延迟到浏览器空闲期注入，避免解析卡顿；切换左侧模块时滚动回到页首；DOCK_VERSION 升至 0.7.0。

## v0.6.0 — 2026-08-24

模型设置、图片理解代理与桌面伙伴三项核心体验升级；新增飞书任务推送，并修正会话界面与全屏动效的交互问题。

### 新增

- **飞书群机器人推送**：任务结束时可向飞书群机器人发送交互卡片；与钉钉并列配置，支持独立 Webhook、连通测试，并跟随「完成 / 异常」通知开关。
- **模型图片能力由用户声明**：模型设置中的「图片」勾选可直接将原图交给该模型；运行时探测仅作候选提示，不再擅自否定用户的已知能力。

### 改进

- **模型设置更及时、可读**：进入页面自动重新读取 Provider 目录，顶部新增刷新按钮；图片理解代理候选默认折叠，只显示已解析且声明图片能力的模型，并明确其未经联网实测的边界。
- **桌面伙伴重构为实时人物动作**：按推理、正文输出和工具调用的活动脉冲调节节奏；收紧为双工位，左侧检索、右侧编码。人物补齐头颈、肩肘腕与坐姿比例，椅子和身体始终面向桌面；思考时微抬头、单手托下巴，输出时键入，检索时转头扫视。
- **任务动效反馈增强**：呼吸光点增加阶段色、辉光和扩散光晕，频率随吞吐变化；主题切换可即时更新面板信息。

### 修复

- **修复会话输入区余额 / 用量 chips 在会话底部换行错位**：宿主输入卡 `.row` 是 `flex-wrap:wrap` 且换行优于收缩，chips 一长会整行变宽，把右侧模型选择器/发送键挤到第二行（左右错位）。此前仅把宽度上限设为 `min(280px,36cqw)` 仍偏宽——标准 780px 输入卡（行内容约 762px）下「add 钮 + 工作区选择器 + chips」与右侧「模型选择器 + 发送键」合计已超宽，即便 chips 未截断也会换行。本次把 chips 行上限收紧到 `min(189px,25cqw)`、内距收紧为 `padding 2px 6px`、`gap 2px`，实测余额 + 用量两 chip 完整显示、不再换行；超限仍用省略号截断（完整值在 title 悬浮提示），不会悬出输入卡圆角。
- 修复全屏星点、极光、火花和彩带等装饰子元素可能遮挡页面点击的问题。

## v0.5.0 — 2026-08-23

任务动画正式落地：8 种氛围动效 + 桌面伙伴 3D 场景、完成通知与提示音、钉钉群机器人推送、交互反馈层；功能开关持久化；余额页与开关交互全面美化。

### 新增

- **任务动画模块**（`features/animation/`，宿主 + 客户端两半）：
  - **运行动画**（任务进行中才出现，速度随 Token 吞吐自动加快）：流光细线 / 呼吸光点 / 轨道光环 / 环屏巡航 / 代码雨 / 星野（弯月 + 流星）/ 极光 / 桌面伙伴，共 8 种氛围模式，模式卡带缩微实时预览；
  - **桌面伙伴**：纯 CSS 3D 长方体拼装的具象人物（眉/眼/鼻/耳/头发/卫衣/坐姿腿脚鞋）坐镇四屏工位，流级实时同步任务阶段——思考仰头冒泡、输出低头敲键盘（中屏代码滚动）、查资料头部左右扫视；整卡可拖到屏幕任意位置（位置持久化），右下角手柄 / 面板预设（紧凑/默认/加大/特大）/ 滑块可缩放 85%~220%（自动保存），键盘方向键微调，适配 `prefers-reduced-motion`；
  - **完成通知**：右上角通知卡片栈（标题/模型/耗时/回合步骤/Token/摘要/结束原因），完成与异常分别开关、停留时长可选、浏览器系统通知（仅页面后台推送）；
  - **提示音**：6 种 WebAudio 合成音效（清脆双音/叮/金币/钟声/脉冲/琶音），完成与异常各配一音，可单独试听，无音频文件依赖；
  - **钉钉推送**：任务结束由宿主直发 markdown 到群机器人（浏览器关着也能推），Webhook 校验 + 连通测试，事件跟随完成/异常开关；
  - **交互反馈层**（与动画开关独立）：发送消息→纸飞机飞出、新会话→火花迸发、切换会话→流光掠过、切换工作目录→全屏微光、任务开始→底部光涌、任务完成→顶部彩带；
  - 新模块契约 **Overlay**：功能描述符可挂全局浮层，外壳统一渲染（dockBridge 外部功能同样支持）。
- **功能开关持久化**：每功能启停写入浏览器本地存储，刷新 / 重启后保持上次状态。
- **桌面伙伴大小可调**：面板页预设 + 滑块，浮层拖拽手柄，配置经宿主持久化。

### 改进

- **模型余额页美化**：余额改主数字卡（大号金额 + 赠送/充值分色小字，多币种横排），配额维度改进度条，模型 chips 默认折叠，Provider 按可查余额排序，连接信息弱化为单行小字。
- **开关交互统一**：状态与操作视觉分离（圆点 + 文字标识「运行中/已停用」，启停改 iOS 滑动开关），统一应用到首页卡片 / 弹层页脚 / 设置页 / 动画页 / 会话页小控件，补 `role=switch` 无障碍语义。
- **动效可见度加强**：代码雨 / 星野 / 极光加亮加浓，交互反馈元素放大带辉光，克制不干扰。
- 桌面伙伴场景精修：桌子缩短人物居中、显示器增至四台（扇形 + 竖屏叠放）、屏幕改整体向上滚动的无缝代码流、人物更拟人（圆角头身、脖颈、帽兜、桌上咖啡杯）。

### 文档

- README 增补任务动画完整使用说明（8 种动效 / 通知 / 提示音 / 钉钉推送 / 常见问题）与功能截图。
- 明确插件 token 消耗边界：默认零消耗；唯一主动调 AI 的图片理解代理默认关闭，模型设置页加黄色警示。

## v0.4.1 — 2026-08-22

- 修复：余额 chip 不随模型切换（改订阅会话级模型目录服务，选择器切换即时推送）。
- 修复：用量 RPC 405/404 降级为「宿主进程是旧版本，重启 dsh web 后重试」提示。
- npm 详情页公开化。

## v0.4.0 — 2026-08-22

- **模块化架构**：功能拆进 `features/` 独立模块（host.js 宿主半部 + view.js(x) 客户端视图），可单独提取成包发布，又可经 dockBridge 装回面板。
- **用量记录**：全部 LLM API 调用记账——秒级时间筛选、Token/费用统计（峰谷计价 + 官网价目自动同步）、分组汇总、明细检索与 CSV 导出。
- **会话区随身小控件**：余额 / 用量 chips 挂模型选择器左侧，点击定位功能坞对应页；每功能可设「会话页显示/隐藏」。
- 功能坞菜单次序重排；README 完善安装与使用说明。
